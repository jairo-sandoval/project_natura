const json = {
    text: 'nuevo texto',
    important: false
}

const resousecer = (subinfo) => {
    const mesaje = subinfo
    return mesaje
}


const text = `Los algoritmos ${resousecer('hola mundo ')}son metodos que cada uno crea para solucionar problemas con los requerimientos que este nesesite`

            console.log(text);